{
  "prd_name": "kbutillib-genome-skill-set",
  "default_max_retries": 0,
  "phases": [
    {
      "name": "Implementation",
      "parallel_group": [
        {
          "task_id": "kbutillib-expert-refresh",
          "lane": "in_context",
          "role": "developer",
          "prompt": "Refresh the kbutillib-expert skill in KBUtilLib by applying the 8 approved gap-fixes documented in `agent-io/prds/kbutillib-genome-skill-set/fullprompt.md` under section: `## Implementation Decisions` → `### kbutillib-expert refresh — exact gap fixes`.\n\nFiles to edit (sources of truth — runtime `.claude/commands/` is sync-managed and gitignored, do not touch):\n- `agent-io/skills/kbutillib-expert.md`\n- `agent-io/skills/kbutillib-expert/context/module-reference.md`\n- `agent-io/skills/kbutillib-expert/context/api-summary.md`\n- `agent-io/skills/kbutillib-expert/context/patterns.md`\n\nCRITICAL: Fix #1 is INVERTED from the original audit. `shared_env_utils.py:198` marks `get_config(section, key)` as deprecated in favor of `get_config_value(key_path)` with dot notation. The correct fix is to ensure every example uses `kbu.env.get_config_value(\"section.key\")` and to document `get_config()` exactly once as deprecated/INI-compatibility-only. The 2026-06-02 confront round 1 (stall #4) caught this.\n\nFor every fix, verify the underlying source-of-truth claim by reading the relevant `src/kbutillib/` file BEFORE writing the fix. Specifically:\n- Fix #2 (KBJobUtils API): read `src/kbutillib/kb_job_utils/utils.py` and `src/kbutillib/kb_job_utils/__init__.py` to enumerate the real public methods (`run_job`, `cancel_job`, `submit_chain`, `cleanup`, `start_watcher`, etc.). Replace fabricated method names accordingly.\n- Fix #3 (undocumented kb_genome_utils methods): read `src/kbutillib/kb_genome_utils.py` and add module-reference + api-summary coverage for `load_kbase_gene_container`, `object_to_features`, `get_ftr`, `ftr_to_aliases`, `alias_to_ftrs`, `object_to_proteins`, `add_annotations_to_object`, `load_genome_from_local_files`, `aggregate_taxonomies`, `create_synthetic_genome`, `calculate_gc_content`, AND the new methods being added by the parallel task (`save_genome_object`, `save_assembly_from_fasta`, `save_genome_with_assembly`, `validate_genome`, `build_genome_from_fasta_gff`).\n- Fix #8 (CLI chapter): read `src/kbutillib/cli/` and `src/kbutillib/__main__.py` to enumerate actual subcommands. Do NOT invent commands. If `kbu jobs` or `kbu jobdaemon` exist on current main, document them; if they don't, document only what's present.\n\nFix #6 (cross-reference): add `/kbase-genome-expert` to the Related Skills section of `kbutillib-expert.md` even though the parallel task creates that skill file. The slug is fixed (`kbase-genome-expert`).\n\nAfter editing, run these sanity-check greps from the repo root and confirm zero matches:\n- `grep -nE 'kbu\\.env\\.get_config\\(' agent-io/skills/kbutillib-expert.md agent-io/skills/kbutillib-expert/context/*.md` (except the deprecation note, which may contain one)\n- `grep -nE 'kbu\\.jobs\\.(submit|cancel|refresh)\\(' agent-io/skills/kbutillib-expert.md agent-io/skills/kbutillib-expert/context/*.md`\n\nCommit on the task branch with message describing the 8 fixes applied.",
          "success_criteria": "Diff against base shows all 8 gap-fixes applied to the 4 skill files. `grep -nE 'kbu\\.env\\.get_config\\(' agent-io/skills/kbutillib-expert.md agent-io/skills/kbutillib-expert/context/*.md` returns at most one match (the deprecation note). `grep -nE 'kbu\\.jobs\\.(submit|cancel|refresh)\\(' agent-io/skills/kbutillib-expert.md agent-io/skills/kbutillib-expert/context/*.md` returns zero matches. Related Skills section of kbutillib-expert.md includes `/kbase-genome-expert`. CLI chapter in api-summary.md documents only subcommands that exist in `src/kbutillib/cli/` or `src/kbutillib/__main__.py` (no fabricated commands).",
          "repos": ["KBUtilLib"],
          "depends_on": [],
          "machine": null,
          "max_retries": 0
        },
        {
          "task_id": "kbase-genome-expert-and-utils",
          "lane": "in_context",
          "role": "developer",
          "prompt": "Implement the new kbase-genome-expert skill plus supporting code in KBUtilLib, per `agent-io/prds/kbutillib-genome-skill-set/fullprompt.md`.\n\n## Code changes\n\n1. **Extend `src/kbutillib/kb_genome_utils.py`** — add 5 new methods to the `KBGenomeUtils` legacy class (NOT to KBGenomeUtilsImpl directly; the Impl delegates via `__getattr__`):\n   - `save_genome_object(self, genome_dict, workspace, name) -> str` — direct Workspace transport via `self.save_ws_object(name, workspace, genome_dict, 'KBaseGenomes.Genome')`; return `'ws_id/obj_id/version'` from the returned info tuple. `save_ws_object` is inherited from KBWSUtils.\n   - `save_assembly_from_fasta(self, fasta_path, workspace, name, *, wait=True, timeout=600) -> str` — EE2 transport via `self._jobs.run_job(method='AssemblyUtil.save_assembly_from_fasta', params=[{'file':{'path':str(fasta_path)},'workspace_name':workspace,'assembly_name':name}])`. The legacy class does NOT have `_jobs`; this method on the legacy class must raise `RuntimeError('save_assembly_from_fasta requires the composition-facade path; use kbu = KBUtilLib(); kbu.genome.save_assembly_from_fasta(...)')`. The `KBGenomeUtilsImpl` wrapper (see step 2) holds `jobs` and provides the working implementation by overriding `save_assembly_from_fasta` on the Impl class itself rather than delegating.\n   - `save_genome_with_assembly(self, fasta_path, genome_dict, workspace, base_name, *, assembly_suffix='_assembly') -> tuple[str, str]` — orchestrator; shallow-copy genome_dict before splicing assembly_ref.\n   - `validate_genome(self, genome_dict, *, require_assembly_ref=True) -> list[str]` — schema check per PRD § Implementation Decisions → validate_genome. The `require_assembly_ref` kwarg was added per 2026-06-02 confront stall #9.\n   - `build_genome_from_fasta_gff(self, fasta_path, gff_path=None, *, scientific_name, taxonomy, genetic_code=11, source='User', source_id=None) -> dict` — constructor per PRD § Implementation Decisions → build_genome_from_fasta_gff. Implement the GFF3→KBase mapping EXACTLY as specified there (ID derivation, location tuple shape with 1-based inclusive start, strand encoding, CDS-vs-gene treatment, DNA/protein/MD5 population, alias parsing, CDS linking).\n\n2. **Update `KBGenomeUtilsImpl.__init__`** in same file: accept `(env, ws, jobs, **kwargs)`; store `self._jobs = jobs`. Add an explicit `save_assembly_from_fasta` method on `KBGenomeUtilsImpl` (overriding `__getattr__` delegation) that uses `self._jobs.run_job(...)`, polls for terminal state, and returns the value of result key `assembly_ref`. **Before implementing, verify against the AssemblyUtil KIDL spec that the return key is still `assembly_ref`**; if changed, raise to the human before proceeding.\n\n3. **Update `src/kbutillib/toolkit.py`** — change the `genome` property to construct `KBGenomeUtilsImpl(self.env, self.ws, self.jobs)`. Order matters: ensure `jobs` is also a lazy property and that triggering `genome` doesn't break the existing `test_composition_smoke.py` chain.\n\n## Tests\n\nCreate `tests/test_kb_genome_utils_save.py` with the cases enumerated in PRD § Testing Decisions: validate_genome happy + sad paths (missing assembly_ref, contig length mismatch, unknown contig location, duplicate IDs), build_genome_from_fasta_only + with-gff smoke tests, save_assembly_from_fasta_legacy_raises, save_genome_object_returns_ref_format (mock self.ws.save_ws_object). EE2 round-trip tests marked `@pytest.mark.integration` and skipped by default. Reuse style from `tests/test_composition_smoke.py`.\n\n## New skill file\n\nCreate `agent-io/skills/kbase-genome-expert.md` with YAML frontmatter (`name: KBase Genome Expert`, `description: Expert on saving, loading, validating, and manipulating KBase Genome objects from notebook contexts`, `scope: domain`) and the 7 body sections enumerated in PRD § Implementation Decisions → Skill file layout. Every API name referenced must exist in source — verify via `grep` before committing the skill file.\n\n## Final checks\n\n- `pytest tests/test_kb_genome_utils_save.py -q` passes (unit tests; integration skipped).\n- `grep` every API name in the new skill file against `src/kbutillib/` — zero fabricated methods.\n- Commit on task branch with message describing the 5 new methods + skill file.",
          "success_criteria": "5 new methods exist on `KBGenomeUtils` in `src/kbutillib/kb_genome_utils.py`. `KBGenomeUtilsImpl.__init__` signature is `(self, env, ws, jobs, **kwargs)`. `toolkit.py` `genome` property passes `jobs` to the Impl constructor. `tests/test_kb_genome_utils_save.py` exists and `pytest tests/test_kb_genome_utils_save.py -q` reports zero failures (integration tests skipped). `agent-io/skills/kbase-genome-expert.md` exists with the required frontmatter and 7 body sections. `kbu.genome.save_genome_object(genome_dict, ws, name)` returns 'ws_id/obj_id/version'. `kbu.genome.validate_genome(genome_dict, require_assembly_ref=False)` accepts a dict missing assembly_ref. `kbu.genome.build_genome_from_fasta_gff(...)` produces a dict passing `validate_genome(..., require_assembly_ref=False)`. Calling `save_assembly_from_fasta` on a bare `KBGenomeUtils()` raises `RuntimeError` mentioning the facade. Every API name in `kbase-genome-expert.md` resolves via grep to a real symbol in `src/kbutillib/`.",
          "repos": ["KBUtilLib"],
          "depends_on": [],
          "machine": null,
          "max_retries": 0
        }
      ]
    }
  ]
}
